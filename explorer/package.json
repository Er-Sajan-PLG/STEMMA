{
  "name": "stemma",
  "version": "1.0.0",
  "private": true,
  "description": "stemma — the STEM-pop 3D knowledge graph explorer",
  "type": "module",
  "scripts": {
    "predev": "node scripts/sync-export.mjs",
    "dev": "vite",
    "prebuild": "node scripts/sync-export.mjs",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "verify": "node scripts/verify-graph-projection.mjs"
  },
  "dependencies": {
    "3d-force-graph": "^1.80.0",
    "three": "^0.185.0"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "esbuild": "^0.21.5",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
