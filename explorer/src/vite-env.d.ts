/// <reference types="vite/client" />

interface ImportMetaEnv {
  /** Optional override for the feedback link (defaults to the repo's feedback issue form). */
  readonly VITE_FEEDBACK_URL?: string;
}
