{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://stemma.example/schema/concept.schema.json",
  "title": "STEMMA canonical entity",
  "description": "Schema for the YAML frontmatter of one canonical knowledge entity (v0.3). Aligned with STEMMA-SPECIFICATION.md §4.",
  "type": "object",
  "required": ["id", "type", "name", "domain", "status", "definition", "provenance"],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^lhs:[a-z][a-z0-9-]*\\.[a-z0-9][a-z0-9-]*$",
      "description": "Stable identifier, namespace lhs:<domain>.<slug>. Never reused."
    },
    "type": {
      "enum": ["concept", "quantity", "unit", "law", "equation", "misconception", "phenomenon", "model", "experiment", "regime", "observation", "measurement", "classification", "definition", "claim"],
      "description": "Entity type. Core: concept, quantity, unit, law, equation, misconception. Extended: phenomenon, model, experiment, regime, observation, measurement, classification, definition, claim."
    },
    "name": { "type": "string" },
    "domain": { "type": "string", "description": "Subject area, e.g. physics" },
    "status": {
      "enum": ["draft", "machine_validated", "human_reviewed", "canonical", "deprecated", "superseded"]
    },
    "aliases": { "type": "array", "items": { "type": "string" }, "description": "Historical IDs" },
    "deprecated_by": { "type": "string", "description": "Successor ID when deprecated/superseded" },
    "definition": { "type": "string", "description": "Curriculum-agnostic definition" },
    "examples": { "type": "array", "items": { "type": "string" } },
    "equation": { "type": ["string", "null"], "default": null, "description": "Canonical mathematical form using defined symbols (optional)" },
    "symbol": { "type": ["string", "null"], "default": null, "description": "Symbol(s) for the quantity or law (optional)" },
    "unit": { "type": ["string", "null"], "default": null, "description": "SI unit of the quantity (optional)" },
    "common_misconceptions": { "type": "array", "items": { "type": "string" }, "description": "Common false beliefs learners hold; knowledge-layer, not pedagogy (optional)" },
    "learning_objectives": {
      "type": "array",
      "items": { "type": "string" },
      "description": "What the learner should understand, calculate, predict, or explain. Knowledge-layer: intrinsic to the concept, not pedagogy."
    },
    "real_world_applications": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Real-world phenomena and technologies this concept explains or enables."
    },
    "key_experiments": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Classic experiments or demonstrations that establish this concept."
    },
    "provenance": {
      "type": "object",
      "required": ["ai_drafted"],
      "properties": {
        "ai_drafted": { "type": "boolean" },
        "source_kind": {
          "enum": [
            "human-authored",
            "textbook",
            "academic-or-research",
            "institutional",
            "standards-or-specification",
            "ai-assisted-draft",
            "other"
          ],
          "default": null
        },
        "source": { "type": ["string", "null"], "default": null },
        "reviewer": { "type": ["string", "null"], "default": null },
        "reviewed_at": { "type": ["string", "null"], "default": null }
      },
      "additionalProperties": false
    },
    "relationships": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["type", "target"],
        "properties": {
          "type": {
            "enum": [
              "logically_requires",
              "mathematically_requires",
              "part_of",
              "derived_from",
              "special_case_of",
              "generalizes",
              "equivalent_to",
              "applies_to",
              "appears_in_law",
              "related_to"
            ]
          },
          "target": { "type": "string", "pattern": "^lhs:" },
          "note": { "type": "string" }
        },
        "additionalProperties": false
      }
    },
    "extensions": {
      "type": "object",
      "additionalProperties": true,
      "description": "Adaptive metadata seam (ADR-0017). Open at schema level so new knowledge-layer dimensions never hard-fail; the validator enforces that every key is registered in schema/extension-registry.yaml for this object kind."
    },
    "historical": {
      "type": "object",
      "required": ["stated_by", "year"],
      "properties": {
        "stated_by": { "type": "string", "description": "Person/group who first stated or discovered the claim (required when field present)" },
        "year": { "type": "integer", "description": "Year (CE; negative for BCE) of first statement/publication" },
        "where": { "type": "string", "description": "Optional: the work/source where it was stated" },
        "context": { "type": "string", "description": "Optional domain note" },
        "note": { "type": "string", "description": "Optional attribution caveat / uncertainty / contested-origin note" },
        "timeline": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["year", "event"],
            "properties": {
              "year": { "type": "integer" },
              "by": { "type": "string", "description": "Optional; defaults to stated_by" },
              "event": { "type": "string" }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false,
      "description": "Historical scientific attribution (ADR-0018): who first stated this claim and when. Distinct from provenance (record source)."
    }
  },
  "additionalProperties": false
}